import { Typography } from "antd";

const Rules = () => {
  return (
    <Typography.Paragraph>
      尚不支持自定义拆分规则。目前的规则为根少优先、笔顺优先、能连不交、取大优先。
    </Typography.Paragraph>
  );
};

export default Rules;
